<%- include('../layout/header.ejs')%>
    <div class='container'>
        <h1 class='text-center'>
            <%=article.title%>
                <!-- 只有登录并且用户id和作者id相同才显示按钮 -->
                <%if(islogin&&user.id === article.authorId){%>
                    <a href="/article/edit/<%=article.id%>" class="btn btn-info pull-right">编辑</a>
                    <%}%>
        </h1>
        <hr />
        <div>
            <!-- 把=号改成减号解析html标签 -->
            <%-article.content%>
        </div>
    </div>

    <%-include('../layout/footer.ejs')%>